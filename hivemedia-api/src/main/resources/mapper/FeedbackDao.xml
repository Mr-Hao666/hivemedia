<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.hivemedia.dao.FeedbackDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="cn.hivemedia.entity.FeedbackEntity" id="feedbackMap">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="submitFeedback"  parameterType="cn.hivemedia.entity.FeedbackEntity">
        insert into tb_feedback (user_id,content,create_time, update_time)
		    values (#{userId},#{content}, #{createTime}, #{updateTime}
		)

    </insert>

</mapper>