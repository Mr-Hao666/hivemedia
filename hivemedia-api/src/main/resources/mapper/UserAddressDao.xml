<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.hivemedia.dao.UserAddressDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="cn.hivemedia.entity.UserAddressEntity" id="userAddressMap">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="userName" column="user_name"/>
        <result property="phone" column="phone"/>
        <result property="provinceId" column="province_id"/>
        <result property="cityId" column="city_id"/>
        <result property="areaId" column="area_id"/>
        <result property="detailAddress" column="detail_address"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="isDefault" column="is_default"/>
    </resultMap>

    <delete id="deleteReceivingAddress" parameterType="java.lang.Long" >
    delete from tb_user_address
    where ID = #{id}
   </delete>

    <select id="getById" parameterType="java.lang.Long" resultType="cn.hivemedia.entity.UserAddressEntity">
		SELECT * FROM tb_user_address where id = #{id}
  </select>

    <select id="getDefaultAddressByUserId" parameterType="java.lang.Long" resultType="cn.hivemedia.entity.UserAddressEntity">
		SELECT * FROM tb_user_address where user_id = #{userId} and is_default=1
  </select>

  
</mapper>